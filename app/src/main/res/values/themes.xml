<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema da Splash Screen que usa a nova API -->
    <style name="Theme.LouveApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_wrapper</item>
        <item name="postSplashScreenTheme">@style/Theme.LouveApp.Base</item>
    </style>
    <!-- Tema principal do app APÓS a splash, preparado para o edge-to-edge -->
    <style name="Theme.LouveApp.Base" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>