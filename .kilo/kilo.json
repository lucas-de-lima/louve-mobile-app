{
  "$schema": "https://app.kilo.ai/config.json",
  "mcp": {
    "firebase": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "firebase-tools@14.15.2",
        "experimental:mcp",
        "--dir",
        "{{FIREBASE_DIR}}",
        "--only",
        "{{FIREBASE_ONLY}}"
      ]
    }
  }
}
