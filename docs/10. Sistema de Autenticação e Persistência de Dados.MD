# Sistema de Autenticação e Persistência de Dados

## Visão Geral

Este documento detalha o sistema completo de autenticação, criação de contas de usuário e persistência de dados do Louve Mobile App. O sistema implementa uma arquitetura híbrida que combina armazenamento local (DataStore) para usuários não autenticados e sincronização na nuvem (Firebase) para usuários autenticados.

## 🎯 Objetivos do Sistema

### 1. Autenticação Segura
- Login com Google Sign-In
- Integração com Firebase Authentication
- Gerenciamento de sessões de usuário

### 2. Persistência Inteligente
- Dados locais para usuários não autenticados
- Sincronização na nuvem para usuários autenticados
- Transição automática entre modos local/remoto

### 3. Sincronização de Dados
- Favoritos sincronizados entre dispositivos
- Configurações de tema persistentes
- Perfil de usuário gerenciado centralmente

## 🏗️ Arquitetura do Sistema

### Visão Geral da Arquitetura

```
┌─────────────────────────────────────────────────────────────┐
│                    Camada de Apresentação                    │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │   HomeScreen    │  │ FavoritesScreen │  │SettingsScreen│ │
│  └─────────────────┘  └─────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Camada de ViewModel                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │  HomeViewModel  │  │FavoritesViewModel│  │SettingsViewModel│ │
│  └─────────────────┘  └─────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Camada de Repositório                     │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │ FavoritesRepo   │  │ SettingsRepo    │  │  UserRepo   │ │
│  └─────────────────┘  └─────────────────┘  └─────────────┘ │
│           │                    │                    │       │
│           ▼                    ▼                    ▼       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │ DefaultFavorites│  │DefaultSettings  │  │FirestoreUser│ │
│  │ Repository      │  │Repository       │  │Repository   │ │
│  └─────────────────┘  └─────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Camada de Dados                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │ DataStore       │  │   Firebase      │  │  Firebase   │ │
│  │ (Local)         │  │ Authentication  │  │  Firestore  │ │
│  └─────────────────┘  └─────────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### Componentes Principais

#### 1. Sistema de Autenticação
- **FirebaseAuthRepositoryImpl**: Gerencia autenticação com Firebase
- **AuthViewModel**: Coordena operações de login/logout na UI
- **Google Sign-In**: Provedor de autenticação principal

#### 2. Sistema de Persistência
- **DefaultFavoritesRepository**: Repositório mediador para favoritos
- **DefaultSettingsRepository**: Repositório mediador para configurações
- **FirestoreUserRepositoryImpl**: Gerencia dados remotos do usuário

#### 3. Armazenamento Local
- **DataStoreLocalFavoritesRepository**: Favoritos locais via DataStore
- **LocalSettingsRepository**: Configurações locais via DataStore

## 🔐 Fluxo de Autenticação

### 1. Processo de Login

#### Etapa 1: Inicialização do Google Sign-In
```kotlin
// SettingsScreen.kt
val googleSignInLauncher = rememberLauncherForActivityResult(
    contract = ActivityResultContracts.StartActivityForResult(),
    onResult = { result ->
        val task = GoogleSignIn.getSignedInAccountFromIntent(result.data)
        try {
            val account = task.getResult(ApiException::class.java)!!
            val idToken = account.idToken!!
            authViewModel.signIn(AuthCredentials.Google(idToken))
        } catch (e: ApiException) {
            // Tratamento de erro
        }
    }
)
```

#### Etapa 2: Autenticação no Firebase
```kotlin
// FirebaseAuthRepositoryImpl.kt
override suspend fun signIn(credentials: AuthCredentials): Result<Unit> {
    return try {
        when (credentials) {
            is AuthCredentials.Google -> {
                val firebaseCredential = GoogleAuthProvider.getCredential(credentials.idToken, null)
                val authResult = auth.signInWithCredential(firebaseCredential).await()
                
                // Criação automática do perfil do usuário
                val user = authResult.user
                if (user != null) {
                    val userProfileData = mapOf(
                        "uid" to user.uid,
                        "name" to user.displayName,
                        "email" to user.email,
                        "photoUrl" to user.photoUrl?.toString(),
                        "createdAt" to System.currentTimeMillis()
                    )
                    firestore.collection("users").document(user.uid)
                        .set(userProfileData, SetOptions.merge())
                        .await()
                }
                Result.success(Unit)
            }
        }
    } catch (e: Exception) {
        Result.failure(e)
    }
}
```

#### Etapa 3: Criação da Estrutura do Usuário
```kotlin
// FirestoreUserRepositoryImpl.kt
override suspend fun ensureUserStructure(userProfile: UserProfile): Result<Unit> {
    val userId = userProfile.uid
    return try {
        firestore.runTransaction { transaction ->
            val userDocRef = firestore.collection("users").document(userId)
            val userSnapshot = transaction.get(userDocRef)

            if (!userSnapshot.exists()) {
                // 1. Perfil do usuário
                transaction.set(userDocRef, userProfile)

                // 2. Configurações padrão
                val settingsDocRef = userDocRef.collection("users")
                    .document("settings")
                transaction.set(settingsDocRef, UserSettings())

                // 3. Lista de favoritos vazia
                val favoritesDocRef = userDocRef.collection("favorites")
                    .document("hymns")
                transaction.set(favoritesDocRef, mapOf("ids" to emptyList<String>()))
            }
            null
        }.await()
        Result.Success(Unit)
    } catch (e: Exception) {
        Result.Error("Falha ao inicializar dados do usuário.", e)
    }
}
```

### 2. Estrutura de Dados no Firestore

```
/users/{userId}/
├── uid: "string"
├── name: "string"
├── email: "string"
├── photoUrl: "string"
├── createdAt: timestamp
├── users/
│   └── settings/
│       └── themeId: "default_light_theme"
└── favorites/
    └── hymns/
        └── ids: ["1", "15", "23"]
```

### 3. Regras de Segurança

```javascript
// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}
```

**Princípio de Segurança**: Cada usuário só pode acessar seus próprios dados (`/users/{userId}`).

## 💾 Sistema de Persistência Híbrida

### 1. Estratégia de Repositório Mediador

#### DefaultFavoritesRepository
```kotlin
@Singleton
class DefaultFavoritesRepository @Inject constructor(
    private val authRepository: AuthRepository,
    private val localRepository: LocalFavoritesRepository,
    private val remoteRepository: UserRepository
) : FavoritesRepository {

    override fun getFavoriteHymnIds(): Flow<Result<Set<String>>> {
        return authRepository.getCurrentUser().flatMapLatest { user ->
            if (user != null) {
                // Usuário logado: usa repositório remoto
                remoteRepository.getFavoriteHymnIds()
            } else {
                // Usuário deslogado: usa repositório local
                localRepository.getFavorites().map { favorites ->
                    Result.Success(favorites)
                }
            }
        }
    }

    override suspend fun addFavorite(hymnId: String): Result<Unit> {
        val user = authRepository.getCurrentUser().first()
        return if (user != null) {
            remoteRepository.addFavorite(hymnId)
        } else {
            localRepository.addFavorite(hymnId)
        }
    }
}
```

#### DefaultSettingsRepository
```kotlin
@Singleton
class DefaultSettingsRepository @Inject constructor(
    private val authRepository: AuthRepository,
    private val localSettingsRepository: LocalSettingsRepository,
    private val userRepository: UserRepository
) : SettingsRepository {

    override val theme: Flow<String> = authRepository.getCurrentUser().flatMapLatest { user ->
        if (user != null) {
            // Usuário logado: observa configurações do Firestore
            userRepository.getUserSettings().map { result ->
                (result as? Result.Success)?.data?.themeId ?: DefaultTheme.name
            }
        } else {
            // Usuário deslogado: observa configurações locais
            localSettingsRepository.theme
        }
    }

    override suspend fun saveTheme(themeName: String) {
        val user = authRepository.getCurrentUser().first()
        if (user != null) {
            // Usuário logado: salva no Firestore
            userRepository.updateUserSettings(UserSettings(themeId = themeName))
        } else {
            // Usuário deslogado: salva localmente
            localSettingsRepository.saveTheme(themeName)
        }
    }
}
```

### 2. Armazenamento Local (DataStore)

#### Favoritos Locais
```kotlin
@Singleton
class DataStoreLocalFavoritesRepository @Inject constructor(
    @ApplicationContext private val context: Context
) : LocalFavoritesRepository {

    private object PreferencesKeys {
        val FAVORITE_HYMN_IDS = stringSetPreferencesKey("favorite_hymn_ids")
    }

    override fun getFavorites(): Flow<Set<String>> {
        return context.dataStore.data.map { preferences ->
            preferences[PreferencesKeys.FAVORITE_HYMN_IDS] ?: emptySet()
        }
    }

    override suspend fun addFavorite(hymnId: String): Result<Unit> {
        return try {
            context.dataStore.edit { preferences ->
                val currentFavorites = preferences[PreferencesKeys.FAVORITE_HYMN_IDS] ?: emptySet()
                preferences[PreferencesKeys.FAVORITE_HYMN_IDS] = currentFavorites + hymnId
            }
            Result.Success(Unit)
        } catch (e: Exception) {
            Result.Error("Falha ao salvar favorito localmente.", e)
        }
    }
}
```

#### Configurações Locais
```kotlin
@Singleton
class LocalSettingsRepository @Inject constructor(
    @ApplicationContext private val context: Context
) : SettingsRepository {

    private object Keys {
        val APP_THEME = stringPreferencesKey("app_theme")
    }

    override val theme: Flow<String>
        get() = context.settingsDataStore.data.map { preferences ->
            preferences[Keys.APP_THEME] ?: DefaultTheme.name
        }

    override suspend fun saveTheme(themeName: String) {
        context.settingsDataStore.edit { settings ->
            settings[Keys.APP_THEME] = themeName
        }
    }
}
```

### 3. Armazenamento Remoto (Firestore)

#### Configurações do Usuário
```kotlin
override fun getUserSettings(): Flow<Result<UserSettings>> = callbackFlow {
    val userId = currentUserId
    if (userId == null) {
        trySend(Result.Error("Usuário não autenticado."))
        close()
        return@callbackFlow
    }

    val settingsDocRef = firestore.collection("users").document(userId)
        .collection("users").document("settings")

    val listener = settingsDocRef.addSnapshotListener { snapshot, error ->
        if (error != null) {
            trySend(Result.Error("Erro ao observar configurações.", error))
            return@addSnapshotListener
        }

        if (snapshot != null && snapshot.exists()) {
            val settings = snapshot.toObject(UserSettings::class.java) ?: UserSettings()
            trySend(Result.Success(settings))
        } else {
            trySend(Result.Success(UserSettings()))
        }
    }
    awaitClose { listener.remove() }
}
```

#### Favoritos do Usuário
```kotlin
override fun getFavoriteHymnIds(): Flow<Result<Set<String>>> = callbackFlow {
    val userId = currentUserId
    if (userId == null) {
        trySend(Result.Error("Usuário não autenticado."))
        close()
        return@callbackFlow
    }

    val favoritesDocRef = firestore.collection("users").document(userId)
        .collection("favorites").document("hymns")

    val listener = favoritesDocRef.addSnapshotListener { snapshot, error ->
        if (error != null) {
            trySend(Result.Error("Erro ao observar favoritos.", error))
            return@addSnapshotListener
        }

        if (snapshot != null && snapshot.exists()) {
            @Suppress("UNCHECKED_CAST")
            val ids = snapshot.get("ids") as? List<String>
            trySend(Result.Success(ids?.toSet() ?: emptySet()))
        } else {
            trySend(Result.Success(emptySet()))
        }
    }
    awaitClose { listener.remove() }
}
```

## 🔄 Fluxo de Sincronização

### 1. Transição Automática Local ↔ Remoto

#### Cenário: Usuário não autenticado
1. **Favoritos**: Salvos localmente via DataStore
2. **Tema**: Salvo localmente via DataStore
3. **Dados**: Persistem apenas no dispositivo

#### Cenário: Usuário faz login
1. **Autenticação**: Firebase Auth cria sessão
2. **Estrutura**: Firestore cria documentos do usuário
3. **Transição**: Repositórios mediadores mudam para modo remoto
4. **Dados**: Agora sincronizados com a nuvem

#### Cenário: Usuário faz logout
1. **Logout**: Firebase Auth remove sessão
2. **Transição**: Repositórios mediadores voltam para modo local
3. **Dados**: Voltam a ser salvos localmente

### 2. Observação em Tempo Real

#### Firestore Snapshot Listeners
```kotlin
val listener = settingsDocRef.addSnapshotListener { snapshot, error ->
    if (error != null) {
        trySend(Result.Error("Erro ao observar configurações.", error))
        return@addSnapshotListener
    }

    if (snapshot != null && snapshot.exists()) {
        val settings = snapshot.toObject(UserSettings::class.java) ?: UserSettings()
        trySend(Result.Success(settings))
    } else {
        trySend(Result.Success(UserSettings()))
    }
}
```

**Benefícios**:
- Mudanças refletidas instantaneamente na UI
- Sincronização automática entre dispositivos
- Experiência offline-first com fallback para nuvem

## 📱 Experiência do Usuário

### 1. Fluxo de Primeiro Uso

1. **Instalação**: App funciona imediatamente sem login
2. **Personalização**: Usuário pode escolher tema e marcar favoritos
3. **Dados locais**: Tudo salvo no dispositivo
4. **Login opcional**: Usuário pode fazer login quando quiser

### 2. Fluxo de Login

1. **Botão de login**: Disponível na tela de configurações
2. **Seleção de conta**: Google Sign-In mostra contas disponíveis
3. **Autenticação**: Firebase valida credenciais
4. **Criação de perfil**: Estrutura criada automaticamente no Firestore
5. **Transição**: Dados agora sincronizados com a nuvem

### 3. Fluxo de Logout

1. **Botão de logout**: Disponível na tela de configurações
2. **Confirmação**: Usuário confirma a ação
3. **Limpeza de sessão**: Firebase remove autenticação
4. **Transição**: Dados voltam a ser salvos localmente
5. **Persistência**: Dados locais mantidos para uso offline

## 🔍 Detalhes Técnicos

### 1. Injeção de Dependência (Hilt)

#### AppModule.kt
```kotlin
@Module
@InstallIn(SingletonComponent::class)
abstract class RepositoryModule {

    @Binds
    @Singleton
    abstract fun bindAuthRepository(impl: FirebaseAuthRepositoryImpl): AuthRepository

    @Binds
    @Singleton
    abstract fun bindUserRepository(impl: FirestoreUserRepositoryImpl): UserRepository

    @Binds
    @Singleton
    abstract fun bindLocalFavoritesRepository(impl: DataStoreLocalFavoritesRepository): LocalFavoritesRepository

    @Binds
    @Singleton
    abstract fun bindFavoritesRepository(impl: DefaultFavoritesRepository): FavoritesRepository

    @Binds
    @Singleton
    abstract fun bindSettingsRepository(impl: DefaultSettingsRepository): SettingsRepository
}
```

### 2. Gerenciamento de Estado

#### AuthViewModel
```kotlin
@HiltViewModel
class AuthViewModel @Inject constructor(
    private val authRepository: AuthRepository
) : ViewModel() {

    val userProfile: StateFlow<UserProfile?> = authRepository.getCurrentUser()
        .stateIn(
            scope = viewModelScope,
            started = SharingStarted.WhileSubscribed(5000),
            initialValue = null
        )

    fun signIn(credentials: AuthCredentials) {
        viewModelScope.launch {
            authRepository.signIn(credentials)
        }
    }

    fun signOut() {
        viewModelScope.launch {
            authRepository.signOut()
        }
    }
}
```

### 3. Tratamento de Erros

#### Result Pattern
```kotlin
sealed class Result<out T> {
    data class Success<T>(val data: T) : Result<T>()
    data class Error(val message: String, val exception: Exception? = null) : Result<Nothing>()
}
```

**Benefícios**:
- Tratamento consistente de erros
- Mensagens de erro amigáveis para o usuário
- Logs detalhados para debugging

## 🚀 Próximos Passos

### 1. Sincronização Bidirecional
- **Merge de dados**: Combinar favoritos locais com remotos
- **Resolução de conflitos**: Estratégia para dados divergentes
- **Histórico de sincronização**: Log de operações realizadas

### 2. Backup e Restauração
- **Exportação de dados**: Backup dos favoritos e configurações
- **Importação**: Restauração de dados de backup
- **Sincronização entre dispositivos**: Transferência de dados

### 3. Recursos Avançados
- **Perfis múltiplos**: Suporte a múltiplas contas
- **Compartilhamento**: Compartilhar listas de favoritos
- **Análise**: Estatísticas de uso e preferências

## 📋 Checklist de Validação

### Funcionalidade
- [x] Login com Google Sign-In funcionando
- [x] Logout funcionando
- [x] Criação automática de perfil no Firestore
- [x] Transição automática local ↔ remoto
- [x] Sincronização de favoritos em tempo real
- [x] Sincronização de configurações de tema

### Segurança
- [x] Regras de Firestore configuradas
- [x] Autenticação Firebase funcionando
- [x] Dados isolados por usuário
- [x] Tokens de autenticação seguros

### Performance
- [x] Observação em tempo real funcionando
- [x] Transições suaves entre modos
- [x] Cache local para dados frequentes
- [x] Operações assíncronas não bloqueiam UI

### Experiência do Usuário
- [x] App funciona sem login
- [x] Login opcional e simples
- [x] Dados persistem entre sessões
- [x] Sincronização automática e transparente

## 🔗 Arquivos Relacionados

### Autenticação
- `app/src/main/java/com/lucasdelima/louveapp/data/repository/FirebaseAuthRepositoryImpl.kt`
- `app/src/main/java/com/lucasdelima/louveapp/ui/screens/settings/AuthViewModel.kt`
- `app/src/main/java/com/lucasdelima/louveapp/domain/repository/AuthRepository.kt`

### Persistência de Dados
- `app/src/main/java/com/lucasdelima/louveapp/data/repository/DefaultFavoritesRepository.kt`
- `app/src/main/java/com/lucasdelima/louveapp/data/repository/DefaultSettingsRepository.kt`
- `app/src/main/java/com/lucasdelima/louveapp/data/repository/FirestoreUserRepositoryImpl.kt`

### Armazenamento Local
- `app/src/main/java/com/lucasdelima/louveapp/data/repository/DataStoreLocalFavoritesRepository.kt`
- `app/src/main/java/com/lucasdelima/louveapp/data/repository/LocalSettingsRepository.kt`

### Modelos de Dados
- `app/src/main/java/com/lucasdelima/louveapp/domain/model/UserProfile.kt`
- `app/src/main/java/com/lucasdelima/louveapp/domain/model/UserSettings.kt`

### Configuração
- `app/src/main/java/com/lucasdelima/louveapp/di/AppModule.kt`
- `firestore.rules`

---

*Este documento deve ser atualizado sempre que houver mudanças significativas no sistema de autenticação ou persistência de dados.*
